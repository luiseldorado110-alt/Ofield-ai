(() => {
  // Legacy compatibility file kept intentionally empty.
  // The active architectural generator is professional-plan.js,
  // loaded through architecture.js. This avoids duplicate panels,
  // competing click handlers and repeated polling timers.
})();